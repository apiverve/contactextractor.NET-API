<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.1</TargetFramework>
    <Nullable>enable</Nullable>
    <GeneratePackageOnBuild>True</GeneratePackageOnBuild>
    <Title>APIVerve.API.ContactExtractor</Title>
    <Version>1.0.0</Version>
    <Authors>APIVerve</Authors>
    <Company>APIVerve</Company>
    <PackageLicenseFile>LICENSE</PackageLicenseFile>
    <PackageRequireLicenseAcceptance>False</PackageRequireLicenseAcceptance>
    <PackageReadmeFile>Readme.md</PackageReadmeFile>
    <PackageProjectUrl>https://apiverve.com/marketplace/api/contactextractor</PackageProjectUrl>
    <SignAssembly>False</SignAssembly>
    <Description>Contact Extractor is a simple tool for extracting contact data. It returns the contact name, email, and more.</Description>
    <RepositoryUrl>https://github.com/apiverve/contactextractor-NET</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <PackageTags>contact extractor; contact data; contact extractor api; contact data api; contact extractor tool; contact data tool; contact extractor software; contact data software; contact extractor service; contact data service; contact extractor library; contact data library; contact extractor sdk; contact data sdk; contact extractor code; contact data code; contact extractor script; contact data script; contact extractor program; contact data program; contact extractor application; contact data application; contact extractor app; contact data app; contact extractor utility; contact data utility; contact extractor helper; contact data helper; contact extractor helper tool; contact data helper tool; contact extractor helper software; contact data helper software; contact extractor helper service; contact data helper service; contact extractor helper library; contact data helper library; contact extractor helper sdk; contact data helper sdk; contact extractor helper code; contact data helper code; contact extractor helper script; contact data helper script; contact extractor helper program; contact data helper program; contact extractor helper application; contact data helper application; contact extractor helper app; contact data helper app; contact extractor helper tool; contact data helper tool; contact extractor helper utility; contact data helper utility; contact extractor helper</PackageTags>
    <Copyright>©2024 APIVerve. All Rights Reserved</Copyright>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Newtonsoft.Json" Version="13.0.3" />
  </ItemGroup>

  <ItemGroup>
    <None Update="LICENSE">
      <Pack>True</Pack>
      <PackagePath>\</PackagePath>
    </None>
    <None Update="Readme.md">
      <Pack>True</Pack>
      <PackagePath>\</PackagePath>
    </None>
  </ItemGroup>

</Project>
